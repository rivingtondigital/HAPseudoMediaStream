# MediaMTX paths — one per camera configured in the HA Pseudo Camera integration.
# Paths accept RTSP publishers (ffmpeg from HA) and serve readers (Frigate, VLC, etc.)

paths:
  stairs:
    source: publisher

  # Add more paths here and in the HA integration:
  # south:
  #   source: publisher

  # Optional: record while live relay is publishing
  # stairs:
  #   source: publisher
  #   record: yes
  #   recordPath: /recordings/stairs/%Y-%m-%d_%H-%M-%S-%f
  #   recordSegmentDuration: 15s
